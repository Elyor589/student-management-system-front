worker_processes 1;

events { worker_connections 1024; }

http {
    server {
        listen 8094;

        root /app/public;

        location / {
            try_files $uri /index.html;
        }
    }
}